class Solution {
    public long countCommas(long n) {
        if (n <= 999) return 0;

        long tCommas = 0;
        long start = 1000;

        while (start <= n) {
            tCommas += n - start + 1;
            start *= 1000;
        }

        return tCommas;
    }
}